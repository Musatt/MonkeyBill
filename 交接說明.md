# 猴子分帳本 — 交接說明（2026-08-23 改版）

## 這次改了什麼

從「單一 3156 行 `.jsx` ＋ 手工 `tsc` 編譯成 `index.html`」改成標準的 **React + Vite** 專案，
拆成多個檔案、加上回歸測試與 GitHub Actions 自動部署，並修掉一批 bug。
架構與資料模型的細節看 `README.md`，這份只記「改了什麼、為什麼」。

## 修掉的 bug

### 1. 讀取失敗會整包蓋掉資料庫（最嚴重）

舊版 `readOnce()` 把錯誤吞掉一律回 `null`，所以「讀到空資料庫」和「讀取失敗」在程式眼裡一樣。
兩次都 null 之後就認定是全新資料庫，`buildSeedData()` 建範例資料直接寫回 `id='main'`——**所有人的帳沒了，畫面卻一切正常**。
只要 Supabase 回 5xx、RLS 設定改壞或 key 失效就會觸發。

現在 `supabaseGet()` 讀取失敗一定 `throw`，只有「HTTP 200 且確定沒有那一列」才會 seed；
讀不到就顯示錯誤畫面，不顯示也不寫入任何東西。

### 2. 分帳的進位差額正負號寫反

`computeItemAllocation()` 把進位多出來的錢從最大付款人身上「減」掉，方向反了，
導致 `sum(balances) !== 0`，誤差還變成兩倍。1000 元均分 3 人時總和是 -0.04 而不是 0。
改成 `+ excess` 之後所有情境都精準歸零，`npm test` 有 22 條測試守住這個不變量。

### 3. 其他

| 問題 | 修法 |
|---|---|
| `todayStr()` 用 UTC，凌晨 0~8 點記帳會記到前一天 | 改用本地時區 |
| 首頁群組卡把已刪除成員也算進「N 位成員」 | 只算未刪除的 |
| 已刪除成員的名字被永久占用，加不回來也找不到 | 輸入同名時提示可以「復原」，接回原本的 id 與歷史 |
| 剛編輯完 3 秒內按「同步」完全沒作用，卻照樣轉圈 | 改用未存佇列判斷，同步鍵一定會真的去讀 |
| 存檔失敗完全無聲，下次輪詢還會把本地修改蓋掉 | 加 `SaveBanner` 明確提示 + 重試；有未存資料時不拉遠端 |
| 一次操作送出兩次 POST（`setData` updater 裡有 side effect） | 寫入移出 updater，改由佇列串行處理 |
| 兩人同時記帳會整包互相蓋掉 | 改成「先讀雲端 → 只套上自己動過的那幾筆 → 寫回」 |
| 正負號位置不一致（`+TWD 250` vs `TWD -250`） | 統一成 `-TWD 250` |
| 金額小數位混亂（列表吃掉小數、個人分攤固定 2 位） | 專案內主幣別一律用 `settlementDecimals` |
| `formatMoney(undefined)` 印出 `TWD NaN` | 非數字顯示 `TWD —` |
| 手機返回鍵直接關掉 App | 加 hash 路由，返回鍵逐層退回，網址也可以分享 |
| 重新整理就忘記你是誰／要重打群組密碼 | 存在 localStorage |
| 統計頁金額欄固定 76px 會折行（`JPY 1,280,000`） | `nowrap` + 自動寬度 |
| 統計頁六個分類永遠都在，0 也佔一列 | 只顯示有金額的，其他收合 |
| 長條圖長度和 % 基準不同 | 都改用「佔總額」 |
| 複製/編輯/刪除 只有 40×33px、間距 2px | 高度 44px、間距 10px、刪除標紅 |
| 日期時間下拉沒有箭頭，看起來像純文字 | 補上箭頭 |
| 打開表單不會回到頂端；儲存鍵在最底部 | 換頁捲到頂端 + 儲存列黏底 |
| 付款人顯示成「猴子 2000」（無幣別、單人時重複） | 「付款：猴子」／多人才列金額 |
| 淨額 0 仍寫「別人該還你這麼多」 | 顯示「已結清」 |
| 成員被移出專案後統計頁下拉會空白 | 自動退回名單上的人 |
| `CurrencySelect` 只在掛載時判斷自訂幣別 | 改成由 value 推導 |
| CSV 檔名沒過濾非法字元、缺原始幣別與匯率欄 | 都補上 |

## 還沒做 / 已知限制

- **逐筆合併不是 CRDT**：A 刪掉某筆、B 同時編輯同一筆，B 的寫入會把它加回來。人不多的情況下可接受。
- **群組密碼是明文**存在 anon 可讀的 Supabase，萬能密碼 `0000` 也在前端。這個鎖只擋手滑，不是存取控制。
- 沒有 PWA manifest／加到主畫面。
- 時間選擇仍然是 5 分鐘為單位。

## 部署前要做的一次性設定

GitHub repo → Settings → Pages → Source 改成 **GitHub Actions**。
改完之後，repo 根目錄那個手工編譯的 `index.html` 就沒有作用了，可以刪掉。
